"use client";
import { useState } from "react";
import { Check, Github, UserRound } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { useAppStore } from "@/store";
import { toast } from "sonner";

export default function SettingsPage() { const user = useAppStore((s) => s.user); const [name, setName] = useState(user || "Alex Morgan"); return <AppShell title="Settings"><main className="content"><div className="page-heading"><div><div className="eyebrow">Your account</div><h1>Settings</h1><p>Make Vibe work the way you do.</p></div></div><div className="panel" style={{ marginBottom: 18 }}><div className="panel-heading"><h2><UserRound size={17} style={{ verticalAlign: "middle", marginRight: 8 }} /> Profile</h2></div><form className="form-grid" onSubmit={(e) => { e.preventDefault(); toast.success("Profile updated"); }}><div className="field"><label htmlFor="name">Display name</label><input id="name" value={name} onChange={(e) => setName(e.target.value)} /></div><div className="field"><label htmlFor="email">Email address</label><input id="email" value="alex@example.com" disabled /></div><button className="button button-primary" style={{ width: "fit-content" }}>Save changes</button></form></div><div className="panel"><div className="panel-heading"><h2><Github size={17} style={{ verticalAlign: "middle", marginRight: 8 }} /> GitHub connection</h2><span className="status status-ready"><Check size={12} /> Connected</span></div><p style={{ color: "var(--muted)", fontSize: 13, lineHeight: 1.6 }}>Push your generated static sites to GitHub without leaving your workspace.</p><button className="button button-secondary" onClick={() => toast("GitHub connection is already active")}>Manage connection</button></div></main></AppShell>; }
